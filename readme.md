## Spring Boot + Vaadin

### Compile and Run

```
mvn clean install
mvn spring-boot:run
```

### Test

Once you run the application, access the only endpoint in browser `http://localhost:8080/vaadin/login`
